---
run_id: "29815378203"
run_name: "PyTorch CI Trigger - PR #190163 (closed)"
run_url: https://github.com/Ascend/pytorch/actions/runs/29815378203
analyzed_at: 2026-07-21T09:23:57Z
fingerprint: e264f57e38d0d1fdcdcb06958239b8a418afdb079d23b0b518e50d208509d236
failed_jobs:
  - forward / build / pytorch_and_torch-npu_build
---

# CI Build 失败分析报告

## 运行信息

| 字段 | 值 |
|------|-----|
| Run ID | 29815378203 |
| Run Name | PyTorch CI Trigger - PR #190163 (closed) |
| 触发事件 | `repository_dispatch` (event_type=`pull_request`) |
| 触发仓库 | `Ascend/pytorch` (downstream)，上游 PR `pytorch/pytorch#190163` (来自 fork `ZhaoqiongZ/pytorch`) |
| Head SHA (上游 PR) | `75604cd11166d622cb55f7a177f7e9eaa3e54f99` |
| Head 分支 | `fix-20260716-test_emulate_precision_casts_float16` |
| 触发时间 | 2026-07-21T08:43:14Z |
| PR 关闭时间 | 2026-07-21T08:43:06Z |
| 失败时间 | 2026-07-21T08:52:39Z (首个 step 失败) |
| 运行链接 | https://github.com/Ascend/pytorch/actions/runs/29815378203 |

## 失败概览

| # | 失败 Job | 失败 Step | 错误类型 |
|---|----------|-----------|----------|
| 1 | forward / build / pytorch_and_torch-npu_build | Extract PyTorch source (Step 6) | 解压上游源码 tarball 时 tar 报 `Unexpected EOF` (exit 2) |
| 2 | forward / build / pytorch_and_torch-npu_build | Summary (Step 18) | 前置 step 失败后 `torch_npu` 目录未 checkout，`git -C torch_npu rev-parse` 报 `cannot change to 'torch_npu'` (exit 128) |

两个失败存在因果关系：步骤 1 失败导致后续步骤（含 `actions/checkout` 下载 `torch_npu`）未执行，从而步骤 2 在 `git -C torch_npu` 时找不到目录。**根因均为同一事件：从 Azure Blob 下载 `pytorch-source-75604cd111.tar.gz` 时收到了截断/损坏的文件。**

## 详细分析

### 失败 1: Extract PyTorch source

**失败 Step**: `Extract PyTorch source`（Step 6）

**关键错误日志**：
```
2026-07-21T08:52:43.7165893Z Preparing to download the following artifacts:
2026-07-21T08:52:43.7165893Z - pytorch-source-75604cd111.tar.gz (ID: 8489278304, Size: 4701360206, Expected Digest: sha256:b593fce9a2ea5992a3c8c01c01eda6ad35f8f18cab6b12184101c48c8ecda421)
2026-07-21T08:52:44.3179750Z Starting download of artifact to: /__w/pytorch/pytorch
...
2026-07-21T09:09:03.1518944Z ##[group]Run tar -xzf pytorch-source-75604cd111.tar.gz
2026-07-21T09:09:41.1953513Z
2026-07-21T09:09:41.1953979Z gzip: stdin: unexpected end of file
2026-07-21T09:09:41.1954874Z tar: Unexpected EOF in archive
2026-07-21T09:09:41.1955262Z tar: Unexpected EOF in archive
2026-07-21T09:09:41.1960607Z tar: Error is not recoverable: exiting now
2026-07-21T09:09:41.2630237Z ##[error]Error: failed to run script step: Error: command terminated with non-zero exit code: command terminated with exit code 2
```

**根因分析**：

`pytorch-source-75604cd111.tar.gz` 来源于前序 job 上传的 artifact（`actions/upload-artifact@v4`），对应上游 PyTorch 仓库 `pytorch/pytorch` 的 commit `75604cd11166d622cb55f7a177f7e9eaa3e54f99`，声明大小 4,701,360,206 字节（~4.4 GB），期望 SHA256 `b593fce9a2ea5992a3c8c01c01eda6ad35f8f18cab6b12184101c48c8ecda421`。

实际下载到 `/__w/pytorch/pytorch/pytorch-source-75604cd111.tar.gz` 的文件**不完整**：
- tarball 头能正常读取（`tar -xzf` 进入了文件流）
- 但在解压过程中 `gzip` 报 `unexpected end of file`，说明 gzip 流在某个中间点被截断
- 紧随其后的 `tar: Unexpected EOF in archive` 表明 archive 结构不完整
- 整个解压过程耗时 ~38 秒（09:09:03 → 09:09:41），符合从 Azure Blob 拉取 ~4 GB 数据的时长，说明下载已完成，但内容已损坏

可能原因（按可能性排序）：
1. **上传侧截断** — 上游 `pytorch/pytorch` 的 `pytorch_ci_trigger.yml`/`forward` job 中执行 `actions/upload-artifact@v4` 时，runner 在上传完成前被驱逐或网络中断，导致部分内容写入但 SHA256 digest 仍按本地计算通过（注：GitHub Actions artifact upload 的 digest 在客户端计算，不会因上传失败而失败）。
2. **下载侧截断** — Ascend/pytorch 自建 runner（`/home/runner/k8s/index.js`）从 Azure Blob (`productionresultssa5.blob.core.windows.net`) 下载时被截断。日志中未出现网络错误（`200 OK` 流式下载），但 gzip 流末段缺失。
3. **Blob 存储损坏** — Azure Blob 自身发生数据损坏（极低概率，Azure 有内部校验）。

由于 pytorch source tarball 解压失败，整个 `pytorch_and_torch-npu_build` job 实质上**没有进入编译/链接阶段**——没有 C++ 编译错误、没有 CMake 错误、没有 NPU 相关错误。后续 `Compile torch`、`Compile and install torch_npu` 等步骤因前置失败而未执行。

**源码交叉引用**（确认本 PR 与 build 失败无关）：

被构建的 PR `pytorch/pytorch#190163`（head SHA `75604cd111`）的修改仅涉及测试文件 `test/inductor/test_cuda_repro.py:1702-1711`，加入：

```python
if TEST_XPU and lowp_dtype is torch.float16:
    # To be enabled once triton-xpu emits a constrained fptrunc for
    # `arith.truncf f32 -> f16`, so the fp32 -> fp16 -> fp32 barrier
    # survives SPIR-V/IGC lowering (currently folded to identity).
    # Upstream triton bug: intel/intel-xpu-backend-for-triton#7491.
    raise unittest.SkipTest(
        "XPU: to be enabled after triton-xpu fix "
        "(intel/intel-xpu-backend-for-triton#7491)"
    )
```

该改动**仅在 XPU 运行时跳过测试**，不触碰任何编译路径、`setup.py`、`CMakeLists.txt` 或 torch_npu 的 `*.patch`。本 PR 不会引入 NPU 构建失败——而且 build 在测试运行之前就已经挂掉。

**引入问题的 PR**（AI 基于 git 历史分析，非触发 PR）：
- PR 链接: **不适用 — 基础设施问题**
- 判断依据: 本次失败发生在 `tar -xzf pytorch-source-75604cd111.tar.gz` 这一基础设施步骤，**未涉及任何源码改动**。上游 PR `pytorch/pytorch#190163` 仅修改一个 XPU 测试的 skip 逻辑；Ascend/pytorch 的 `pytorch_ci_trigger.yml` 也无相关 PR 改动。`pytorch_and_torch-npu_build` job 的实际定义位于 `Ascend/pytorch/.github/workflows/receive-trigger.yml@ci-test`（master 分支无此文件），本地 `--depth=1` clone 不含该历史；该 ref 的 commit 也无法通过 `gh api` 直接查询。**整个失败链路是 self-hosted runner → Azure Blob 的 I/O 异常，非 git 提交引入的代码缺陷。**

**修复方案**：

1. **首要动作（短期）**：重跑该 CI run。`repository_dispatch` 类型的 run 不可手动重试（无 `rerun` 按钮），需触发者（pytorchmergebot）重新发送 dispatch，或在 Ascend/pytorch 上手动重放 `client_payload`。如果上游 PR 已 close（`closed_at: 2026-07-21T08:43:06Z`，dispatch 时间为 08:43:14Z，约晚 8 秒），可能需要上游重新开启 PR 触发新的 CI。
2. **根本性加固**：
   - 在 `_build.yml`/`receive-trigger.yml` 的 `Extract PyTorch source` 步骤中，加入**完整性校验**（虽然 `actions/download-artifact@v4` 内部已经校验 SHA256，但本日志显示下载完成且未报 digest mismatch——这意味着 `actions/download-artifact` 没有启用或被绕过，而是 runner 自行从 blob URL 拉取）：
     ```bash
     tar -xzf pytorch-source-75604cd111.tar.gz || {
       echo "::error::Pytorch source tarball is corrupt, retry download"
       exit 1
     }
     ```
   - 如果改用 `actions/download-artifact@v4` 替代裸 `curl`/`wget` 下载，可以利用 GitHub 内置的 digest 校验。`Expected Digest: sha256:b593fce9a2ea5992a3c8c01c01eda6ad35f8f18cab6b12184101c48c8ecda421` 已给出，但 `actions/download-artifact` 不会自动校验——这需要在 download 后 `sha256sum` 对比。
   - 在 `tar -xzf` 前增加 `gzip -t` 健全性检查。
3. **次要问题**：日志中多次出现 `python3: can't open file '/home/runner/_work/_actions/pytorch/test-infra/main/.github/actions/cross-repo-ci-relay-callback/report_callback.py': [Errno 2] No such file or directory`（时间戳 08:52:38、09:09:55）。这表明 `pytorch/test-infra@main` 这个 external action 的 `report_callback.py` 在 main 分支不存在或路径错误。这是 post-job 回调，与 build 失败正交，但说明 `pytorch/test-infra@main` 的 `cross-repo-ci-relay-callback` action 自身有 bug（ref 未 pin 到具体 SHA）。建议在 `receive-trigger.yml@ci-test` 中将该 action pin 到具体 SHA 或 release tag。

**相关源码**：
- `pytorch/test/inductor/test_cuda_repro.py:1699` （上游 PR 修改的测试函数 — **与 build 失败无关**）
- `torch_npu/.gitignore:167` （`c_shim_npu.h` 是 NPU 编译产物，build 未到产物阶段故未生成）

---

### 失败 2: Summary

**失败 Step**: `Summary`（Step 18）

**关键错误日志**：
```
2026-07-21T09:09:43.2197867Z ##[group]Run TORCH_NPU_SHA=$(git -C torch_npu rev-parse HEAD)
2026-07-21T09:09:43.2198456Z TORCH_NPU_SHA=$(git -C torch_npu rev-parse HEAD)
2026-07-21T09:09:43.2198840Z
2026-07-21T09:09:43.2199120Z echo "============================================"
2026-07-21T09:09:43.2199500Z echo "             Build Summary"
...
2026-07-21T09:09:44.2777592Z fatal: cannot change to 'torch_npu': No such file or directory
2026-07-21T09:09:44.3613850Z ##[error]Error: failed to run script step: Error: command terminated with non-zero exit code: command terminated with exit code 128
```

**根因分析**：

Summary 步骤执行 `git -C torch_npu rev-parse HEAD` 以获取 `torch_npu` 仓库的当前 commit SHA，用于在 `$GITHUB_STEP_SUMMARY` 中输出 Build Summary 表格。

`torch_npu` 目录应当由上游 PR build job 的 `Checkout downstream repo (torch_npu)` 步骤（参考 `torch_npu/.github/workflows/_build.yml:69-75`）创建。**该 step 在本次 run 中没有出现在日志中**，因为前置的 `Extract PyTorch source` 已失败（exit 2），runner 在 step 6 后即停止。

时间线：
- 08:52:43 — 开始下载 pytorch-source-75604cd111.tar.gz（声明 4.4 GB）
- 09:09:03 — 进入 step 6 `tar -xzf`
- 09:09:41 — tar 报 EOF，step 6 失败，job 立即进入失败收尾
- 09:09:43 — runner 仍尝试执行 Summary step（可能是 `if: always()` 之类的策略）
- 09:09:44 — `git -C torch_npu` 报 "No such file or directory"

因此 Summary 失败**是 Extract PyTorch source 失败的直接下游效应**，本身不构成独立问题。

**源码交叉引用**：

本地 `_build.yml`（master 分支的占位/模拟版本）显示 Summary step 当前只 echo 信息（`torch_npu/.github/workflows/_build.yml:92-106`）。但实际运行的 `receive-trigger.yml@ci-test` 显然是更复杂的多步 build（包含 `TORCH_NPU_SHA=$(git -C torch_npu rev-parse HEAD)`），该 ref 不在本地 master 中，源码无法在 `$TORCH_NPU_SRC` 内直接看到。

**引入问题的 PR**（AI 基于 git 历史分析，非触发 PR）：
- PR 链接: **不适用 — 前置失败的级联效应**
- 判断依据: Summary 失败是 Extract PyTorch source 失败的直接后果，没有独立的根因。同 Failure 1。

**修复方案**：

修复 Summary 步骤需同步进行两项加固：
1. 解决 Failure 1 的根因（重新触发 run / 校验 artifact 完整性）。
2. （建议）让 Summary step 在前置失败时降级输出而非完全失败——可加 `if: always()` 包装并提供默认值：
   ```bash
   TORCH_NPU_SHA=$(git -C torch_npu rev-parse HEAD 2>/dev/null || echo "UNKNOWN (build failed before checkout)")
   ```
   这样 Summary 仍能输出降级信息，便于人工诊断。

**相关源码**：
- `torch_npu/.github/workflows/_build.yml:92` （master 分支 Summary step 占位实现）
- 实际执行版本 `Ascend/pytorch/.github/workflows/receive-trigger.yml@ci-test` Summary step（不在本地 clone 中）

---

## 总结

| 项 | 内容 |
|----|------|
| 是否阻塞 PR 合入 | 否 — 失败为基础设施问题，与被测 PR (`pytorch/pytorch#190163`) 内容无关 |
| 是否需要修改 pytorch 源码 | 否 |
| 是否需要修改 torch_npu 源码 | 否（除非修复 Summary step 的级联失败逻辑） |
| 主要根因 | Self-hosted runner 从 Azure Blob 下载的 `pytorch-source-75604cd111.tar.gz` 内容不完整（truncated），导致 `Extract PyTorch source` 步骤 `tar -xzf` 失败 |
| 次要问题 | `pytorch/test-infra@main` 的 `cross-repo-ci-relay-callback` action 中 `report_callback.py` 路径错误（与本 build 失败正交） |
| 建议处理 | 重跑该 run；加固 `Extract PyTorch source` 步骤加入 `gzip -t` 健全性检查和 sha256 校验；pin `cross-repo-ci-relay-callback` 到具体 SHA |
